# engine-research inbox

Create-only drop folder. Other session types leave findings here as new files named
`YYYY-MM-DD-<author>-<slug>.md`; this lane folds them into `ENGINE-DOSSIER.md` and deletes them.
Never edit or delete a file you did not create.
